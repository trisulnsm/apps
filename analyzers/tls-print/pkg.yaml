name: TLS Fingerprinter

version: 0.0.6

author: trisul

description: 
   short: Generates and tracks TLS Fingerprint indicators 
   long:  |
      TLS Fingerprinting can be used to identify TLS/SSL client applications including Malware. This app generates and tracks them.

apptype: lua

appresources: jahash.lua tls-fingerprints.json sweepbuf.lua 

dependencies:  
