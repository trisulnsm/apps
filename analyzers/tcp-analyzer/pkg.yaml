name: TCP Analyzer

version: 0.0.2

author: trisul

description: 
   short: TCP Analysis  
   long:  |
      Identify hosts experiencing TCP performance issues by 
      tracking retransmissions,connection setup latency, and timeouts.
      Marks flows experiencing high retransmissions with flow TAG

apptype: lua

appresources: tcp_analyzer.lua  poorquality.lua

dependencies:  
