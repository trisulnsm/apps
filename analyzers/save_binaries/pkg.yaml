name: Save Binaries 

version: 0.0.1

author: trisul

description: 
   short: Extract binary files, PDF, Flash from traffic 
   long:  |
      Install this plugin to extract files that are potentially malicious into a
      directory. This plugin does not trust the Content-Type but relies on 
      reconstructing the file and using the Magic Number.

apptype: lua

appresources: save_exe.lua 

dependencies:  
